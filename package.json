{
  "name": "blog",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format": "prettier -w .",
    "pre-commit": "lint-staged",
    "update": "git commit -m update -a && git push",
    "prepare": "husky"
  },
  "dependencies": {
    "husky": "9.1.7",
    "lint-staged": "15.4.3",
    "prettier": "3.5.2"
  },
  "prettier": {
    "printWidth": 120
  },
  "lint-staged": {
    "*.md": "your-cmd"
  },
  "packageManager": "yarn@1.22.22"
}
